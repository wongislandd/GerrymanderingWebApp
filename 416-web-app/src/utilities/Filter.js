export default class Filter {
    constructor(name, value, minVal, maxVal, step) {
        this.name = name
        this.value = value
        this.minVal = minVal
        this.maxVal = maxVal
        this.step = step
    }
}